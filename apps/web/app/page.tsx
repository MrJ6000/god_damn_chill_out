"use client";

export default function Home() {
  return (
    <main className="mx-auto max-w-5xl px-8 py-16">
      <h1 className="text-4xl font-bold tracking-tight">PolicyVault Sentinel</h1>
      <p className="mt-3 text-muted">
        Assume the AI is compromised. Prove the money is still safe.
      </p>
      <div className="mt-10 rounded-xl border border-line bg-surface p-8">
        <p className="text-sm text-muted">骨架已就緒。</p>
        <p className="mt-2 text-sm text-muted">
          M4：請依 <code className="font-mono text-body">04_你的任務書.md</code> 開始實作五個頁面。
        </p>
      </div>
    </main>
  );
}
